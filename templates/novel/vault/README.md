# vault/ — 小说状态库

给模型加载"上一章发生了什么"的上下文。参考示例小说的结构：

- `角色状态.json` — 每个角色的当前状态（感知阶段/目标/信念）
- `章节状态.md` — 逐章关键事件、张力方向、角色状态、未解悬念（gen.py 自动更新）
- `时间线.md` — 事件时间线
- `人物卡/` — 角色档案

初始可以只放 `章节状态.md` + `角色状态.json` 的空骨架，随生成逐步累积。
